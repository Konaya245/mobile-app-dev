void main() {
  var numbers = []; //integer values only
  numbers = [2, 'ali'];
  numbers.add(2); //insert new value at the end of the list
  numbers.add('ali'); //insert at position 1, value 4
  print(numbers.reversed);
// start next class with no.2 notes part2